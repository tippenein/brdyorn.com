extends layout

append metacontent
  meta(content=tagList)

block content
  title Blog - BrdyOrn.com
  - var i = 1;
  - each post in postList
    if (i % 2 == 0) 
      .blogentry.even
        a(href=post.url) #{post.title}
        span.pull-right #{post.date.getMonth()+1}.#{post.date.getDate()}.#{post.date.getFullYear()}
    else
      .blogentry.odd
        a(href=post.url) #{post.title}
        span.pull-right #{post.date.getMonth()+1}.#{post.date.getDate()}.#{post.date.getFullYear()}
    - i++
block nav
  .blog-nav
    .recent.underline Recent Entries 
    ul.blog-preview
      each post in postList
        li
          a(href=post.url) #{post.title}
    .categories.underline Categories
    ul.globalcats
      each category in categoryList
        .globalcat
          a(href=categoryUrl(category)) #{category}
    .tags-dropdown.underline Tags
    ul.globaltags.hidden
      each tag in tagList
        .globaltag
          a(href=tagUrl(tag)) #{tag}

append jsincludes
  script
    $(".tags-dropdown").toggle(
      function() {
        $(".globaltags").removeClass('hidden');
      }, 
      function() {
        $(".globaltags").addClass('hidden')
      });
    $(".categories").toggle(
      function() {
        $(".globalcats").removeClass('hidden');
      },
      function() {
        $(".globalcats").addClass('hidden');
      });
